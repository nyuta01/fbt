name: fbt-runner-claude-code
version: 0.1.0
protocol: stdio_jsonrpc
command: fbt-runner-claude-code
provides:
  - runner: claude_code.cli
    type: agent
    transform_types: ["agent"]
    artifact_types: ["markdown", "markdown_directory", "text", "directory"]
env:
  - ANTHROPIC_API_KEY
