# Quality Score

Scores are lightweight retrospective signals for agent work. Use 1-5, where 5
means strong and mechanically protected.

| Domain | Score | Evidence | Weak Spot | Next Task |
|---|---:|---|---|---|
| Harness PDCA | 4 | `make verify` includes harness, drift, docs, Go, CLI smoke, e2e knowledge-loop smoke, expanded conformance, dist checks with stamped release metadata, and task-by-task commits | Conformance is still shell-based rather than a reusable fixture harness | `FBT-STATE-001` |
| fbt Spec Coverage | 5 | Core, project config, manifest, state, runner protocol, schema/versioning, runner discovery, security/conformance, usage, examples, init templates, diff/docs, eval/review behavior, and first-pass semantic descriptors exist | Post-MVP schema-generated validation and richer non-text semantic extraction remain future depth | `FBT-REL-002` |
| Go CLI Scaffold | 5 | CLI now exposes help/version with JSON release metadata plus init, parse, plan, build, eval, review, diff, docs, state, artifact, runner diagnostics, doctor, and standard exports with tests and smoke coverage | Future subcommands should be added only with spec-backed acceptance criteria | `FBT-CONF-001` |
| User Workflow UX | 4 | End-to-end demo, MVP-ready docs, plan/build `next:` guidance, `artifact explain`, artifact path/show/history, `review show`, `doctor`, and hint-rich parse diagnostics cover init, parse, plan, build, review, diff, docs, state, and artifact inspection through the CLI | Workflow UX is broad but still lacks richer interactive docs or UI beyond CLI/static docs | `FBT-STD-006` |
| Standards Export | 5 | `docs/standard-export-spec.md` defines OpenLineage, OpenTelemetry, OpenMetadata, redaction, and visualization contracts; `fbt export openlineage` emits OpenLineage RunEvent NDJSON, `fbt export otel` emits OTLP/JSON traces, conformance checks standard keys plus redaction, standard backend visualization recipes are documented, and OpenMetadata was evaluated as an OpenLineage ingestion target instead of a direct core export | Live backend ingestion checks remain external because Marquez, OpenMetadata, OTel collectors, and trace stores are intentionally outside the default runtime | `FBT-REL-002` |
| Runner Discovery | 5 | `internal/plugin`, `internal/runner`, and `fbt runner` tests cover project config, plugin manifests, PATH convention, args/cwd/env process settings, missing commands, missing env diagnostics, protocol initialization, negotiated capability compatibility, and CLI diagnostics | A reusable external runner conformance kit is still needed for third-party authors | `FBT-RUNNER-005` |
| Runner Protocol | 5 | `internal/protocol` and runner tests cover initialize, capability validation, rich runTransform context, JSONL notifications, output candidates, usage events, tool-call events, JSON-RPC errors, cancellation, and build invocation | CLI-agent wrappers still need a documented staging-workspace contract | `FBT-RUNNER-004` |
| Local Runners | 4 | `runners/fake`, `runners/command`, `runners/llm`, and `runners/agent` process-level tests verify protocol compatibility, output candidates, usage/provenance, tool-call events, local build use, and init-generated e2e flow without external services; `make real-llm-smoke` provides an opt-in external runner check | Demo LLM/agent runners can be mistaken for product runners, and external provider/agent adapter authoring is not yet easy enough | `FBT-RUNNER-006` |
| Parser Baseline | 4 | `internal/project`, `internal/config`, and `internal/parser` now have tests for config versioning, artifact aliases, resource refs, path containment, and parse CLI wiring | Parser still emits first-pass diagnostics without schema-generated validation | `FBT-CONF-001` |
| Manifest Graph | 4 | `internal/manifest` and `internal/graph` now test canonical IDs, parent/child maps, deterministic JSON, selectors, CLI manifest writes, and build integration | Planner/build integration still needs fuller state comparison | `FBT-STATE-001` |
| Artifact Descriptors | 5 | `internal/artifact`, `internal/security`, `internal/policy`, and `internal/diff` test file/directory descriptors, artifact version IDs, path containment, symlink rejection, build commit use, text/Markdown artifact comparison, and semantic descriptors for normalized text plus Markdown structure | Richer semantic descriptors for docx, xlsx, and PDF remain outside the lightweight MVP core until extractor runners are defined | `FBT-REL-002` |
| Local State | 5 | `internal/state` tests atomic snapshots, append-only run results, locks, immutable artifact versions, approvals, eval results, policy decisions, CLI inspection, docs, and build commits | Future external state backends remain a spec boundary, not an implementation | `FBT-ARTIFACT-001` |
| Planner Baseline | 4 | `internal/planner` tests run/skip/blocked actions, dirty reasons, selected sets, review/confidence blocking and unblocking, `fbt plan`, and clean build reruns | Dirty reasons remain first-pass until richer run records are produced | `FBT-STATE-001` |
| Security Boundaries | 4 | Security/conformance spec defines trust boundary, path rules, approval blocking, and fake-runner scenarios; tests and conformance smoke cover schema errors, escape, symlink rejection, runner capability mismatch before commit, write-scope denial, size limits, review blocking, approval unblocking, denied-commit state safety, persisted policy decisions, redaction, standard exports, and dirty propagation | Existing CLI-agent adapters such as Codex or Claude Code need an explicit staging-workspace contract before they are safe as first-class runners | `FBT-RUNNER-004` |
