# Quality Score

Scores are lightweight retrospective signals for agent work. Use 1-5, where 5
means strong and mechanically protected.

| Domain | Score | Evidence | Weak Spot | Next Task |
|---|---:|---|---|---|
| Harness PDCA | 4 | `make verify` includes harness, drift, docs, Go, CLI smoke, knowledge-loop smoke, practical examples smoke, runner conformance, product conformance, docs-site build, and dist checks | Product conformance is still shell-based | `FBT-STATE-001` |
| fbt Spec Coverage | 5 | Core, project config, manifest, state, runner protocol, schema/versioning, runner discovery, security/conformance, usage, examples, init templates, diff/artifact inspection, eval behavior inside build, source file-set dirty detection, command transforms, and semantic descriptors are documented | Future CLI additions still need spec-backed acceptance criteria | `FBT-CONF-001` |
| Go CLI Scaffold | 5 | CLI exposes help/version, init, doctor, plan, build, diff, artifact, and standard exports with tests and smoke coverage; `plan/build --force` are explicit rebuild controls; parse/eval/docs/state/runner are intentionally not public commands | Future subcommands should be added only with spec-backed acceptance criteria | `FBT-CONF-001` |
| User Workflow UX | 4 | README, docs site, examples, captured quickstart output, Cobra-backed help/flag handling, stable source-window guidance, read-only `plan`, dependency-ordered `build`, failed build receipts, `--force` rebuild control, reserved no-op config diagnostics, strict YAML field diagnostics, semantic eval boundary guidance, fixed-width status rows, aligned artifact sections, artifact inspection, retention inspection, doctor diagnostics, strict unknown-argument handling, empty-selector failures, standard visualization recipes, CLI-agent adapter safety conformance, real-adapter smoke matrix, and stale-core-boundary drift guards cover the current loop | Standard backend screenshots and live ingestion checks remain opt-in | `FBT-STD-007` |
| Docs Site | 4 | `apps/docs` now presents fbt as a source-files-to-versioned-artifacts tool with quickstart output, manual-generation examples, explicit non-replacement boundaries, remark/Pandoc-style command composition, dbt/DataChain interop, runner scaffolding, runner boundaries, graph selection syntax, explicit force behavior, semantic eval boundary, command-first standard visualization, standard export guidance, and guarded source-of-truth boundary docs | Future screenshots should come from real standard backends | `FBT-STD-007` |
| Standards Export | 4 | OpenLineage and OTLP/JSON export specs, CLI commands, conformance checks, reproducible standard visualization recipes, and no custom backend are covered | Live backend ingestion checks and docs screenshots remain opt-in and not yet captured from real standard backends | `FBT-STD-007` |
| Release Integrity | 5 | Remote resolution, release metadata stamping, dist smoke, signed `v0.1.0` tag, GitHub release assets, and `SHA256SUMS` cover the MVP release baseline | Future releases could automate multi-platform artifact publication in CI | post-MVP |
| Runner Discovery | 5 | Runner config, plugin manifests, PATH convention, args/cwd/env settings, missing command/env diagnostics, doctor output, protocol initialization, capability compatibility, copyable adapter scaffold, adapter packaging docs, and opt-in installed-adapter smoke matrix are covered | Future adapter packages should own their package-specific CI and release docs outside core | post-MVP |
| Runner Protocol | 5 | Protocol tests, runner conformance, scaffold agent-adapter conformance, installed-adapter smoke matrix, authoring docs, adapter docs, semantic eval boundary, optional OpenAI runner tests, output candidate validation, JSON-RPC errors, events, usage, staging-workspace checks, redaction checks, fail-closed markers, and direct-write guards are covered | Future protocol versions need schema-backed compatibility tests | post-MVP |
| Local Runners | 5 | Fake, command, demo LLM/agent, and optional OpenAI runners verify protocol compatibility, source-checkout examples, and demo template flows | Real provider calls are opt-in | `FBT-REL-002` |
| Project Config Surface | 5 | Project config decoding, runners, selectors, policies, transform declarations, reserved-field diagnostics, strict unknown-field diagnostics, documented aliases, and free-form `meta`/`config`/`contract` objects are implemented and documented | Future schema evolution should move from docs-only contracts to generated JSON Schema | post-MVP |
| Parser Baseline | 5 | Project/config/parser tests and conformance cover config versioning, aliases, refs, path containment, line diagnostics, strict YAML unknown fields, reserved removed fields, and CLI parse-error reporting through `plan`/`doctor` | Future richer schema migrations remain out of scope until specified | post-MVP |
| Manifest Graph | 5 | Manifest and graph tests cover canonical IDs, parent/child maps, deterministic JSON, selectors, upstream/downstream graph expansion, ambiguity errors, build-time manifest writes, and build integration | Future state-selection syntax remains out of scope until specified | `FBT-CONF-001` |
| Artifact Descriptors | 5 | Descriptor, security, policy, and diff tests cover files/directories, artifact IDs, path containment, symlink rejection, build commit use, text/Markdown diffs, and semantic descriptors | Richer descriptors for docx, xlsx, and PDF remain runner-owned | `FBT-REL-002` |
| Local State | 5 | State tests cover atomic snapshots, append-only run results, failed invocation and transform receipts, locks, immutable artifact versions, eval results, policy decisions, artifact CLI inspection, read-only retention reports, and build commits | Future destructive cleanup must remain explicit, receipt-aware, and conformance-covered | post-MVP |
| Planner Baseline | 5 | Planner tests cover run/skip/blocked actions, dirty reasons, forced rebuilds, selected sets, dependency-ordered selected DAGs, in-invocation upstream completion, confidence blocking, `fbt plan`, clean build reruns, and forced clean reruns | Future richer state-selection syntax remains out of scope until specified | `FBT-CONF-001` |
| Security Boundaries | 4 | Security/conformance checks cover schema errors, escapes, symlink rejection, runner mismatch before commit, output containment, write-scope denial, size limits, denied-commit safety, persisted policy decisions, redaction, exports, dirty propagation, secret env boundaries, CLI-agent staging/direct-write guards, installed-adapter smoke, and SDK/runtime exclusion from core | OS-level sandboxing stays post-MVP | post-MVP |
