# Quality Score

Scores are lightweight retrospective signals for agent work. Use 1-5, where 5
means strong and mechanically protected.

| Domain | Score | Evidence | Weak Spot | Next Task |
|---|---:|---|---|---|
| Harness PDCA | 5 | `make verify` includes harness, drift, docs, quality-score next-task guards, Go, CLI smoke, knowledge-loop smoke, practical examples smoke, runner conformance, structured product conformance, docs-site build, and dist checks | Future harness work should keep checks deterministic and tied to repository-local state | post-MVP |
| fbt Spec Coverage | 5 | Core, project config, manifest, state, runner protocol, schema/versioning, runner discovery, security/conformance, usage, examples, init templates, diff/artifact inspection, eval behavior inside build, source file-set dirty detection, command transforms, semantic descriptors, status labels, and post-MVP boundaries are documented | Future specs should keep broad open questions out of source-of-truth docs unless they map to a concrete task | post-MVP |
| Go CLI Scaffold | 5 | CLI exposes help/version, init, doctor, plan, build, diff, artifact, and standard exports with tests and smoke coverage; `plan/build --force` are explicit rebuild controls; parse/eval/docs/state/runner are intentionally not public commands | Future CLI additions still need spec-backed acceptance criteria | post-MVP |
| User Workflow UX | 5 | README first-user journey with concrete source/instruction/runner/artifact mappings and input-to-output snippets, docs site, examples, captured quickstart output, Cobra-backed specific help text, flag-scope help, external-command runner framing, actionable common error hints, copy-paste-safe next commands, build output paths and inspection next steps, visible skipped semantic/LLM-judge eval hints, scannable semantic summaries, human-readable retention sizes, local state/artifact directory semantics, standard export summaries, grouped doctor diagnostics, first own-files smoke, daily operations smoke, semantic/evidence-quality boundary smoke, stable source-window guidance, source-file add/change/remove dirty details, read-only `plan`, dependency-ordered `build`, failed build receipts, explicit `plan/build --failed` recovery, `state:failed` selection, `--force` rebuild control, reserved no-op config diagnostics, strict YAML field diagnostics, semantic eval boundary guidance, fixed-width status rows, aligned artifact sections, artifact inspection, retention inspection, strict unknown-argument handling, empty-selector failures, standard visualization smoke, CLI-agent adapter safety conformance, real-adapter smoke matrix, real workflow pilot notes, copied-example guidance, stale-core-boundary drift guards, and a completed CLI polish backlog cover the current loop | Future workflow UX should stay command-composable rather than adding queues or background orchestration | post-MVP |
| Docs Site | 5 | `apps/docs` now presents fbt as a source-files-to-versioned-artifacts tool with quickstart output, manual-generation source-to-artifact evidence, expected artifact excerpts, grouped doctor examples, explicit non-replacement boundaries, remark/Pandoc-style command composition, dbt/DataChain interop, runner scaffolding, runner boundaries, runnable runner/OpenAI/authoring examples, graph selection syntax, explicit force behavior, semantic eval boundary, project-config source/transform examples, command-first standard visualization, standard export summaries, opt-in backend smoke, guarded source-of-truth boundary docs, real workflow pilot notes, and stale public-asset language drift checks | Future docs work should be driven by new first-user confusion | post-MVP |
| Standards Export | 5 | OpenLineage and OTLP/JSON export specs, CLI commands, conformance checks, reproducible standard visualization recipes, opt-in Marquez/OTLP backend smoke, evidence-copy workflow, and no custom backend are covered | Real backend availability remains intentionally external to base verification | post-MVP |
| Release Integrity | 5 | Remote resolution, release metadata stamping, dist smoke, signed `v0.1.0` tag, GitHub release assets, `SHA256SUMS`, end-user core install docs, separate official adapter install commands, adapter version pinning guidance, source/installed/live verification commands, module-scoped adapter tag workflow, signed adapter tag guidance, Go module checksum verification, future adapter binary checksum/signature guidance, adapter release workflow, and release-plan drift checks cover the MVP release baseline | Future binary adapter archives should use the documented checksum/signature flow when they exist | post-MVP |
| Runner Discovery | 5 | Runner config, plugin manifests, PATH convention, args/cwd/env settings, missing command/env diagnostics, doctor output, protocol initialization, capability compatibility, copyable adapter scaffold, example adapter layout under `examples/runner_adapters`, test fixtures under `tests/runner_fixtures`, adapter packaging docs, official adapter design research, monorepo nested-module strategy, provider-free `sdk/go`, remote-installable official adapter modules, module-scoped adapter release tags, optional runner lockfile contract/schema, validator-only lockfile doctor/build diagnostics, lockfile dirty-state participation, `runner-lockfile-spec-check`, `official-adapter-smoke`, clean adapter install smoke, and opt-in installed/live adapter smoke matrix are covered | Future runner discovery work should be driven by real adapter packaging drift rather than adding package-manager behavior to core | post-MVP |
| Runner Protocol | 5 | Protocol tests, explicit 16 MiB JSONL frame limits, bounded/redacted stderr and exit diagnostics, failed-receipt protocol diagnostics, large-message core and SDK tests, runner conformance, scaffold agent-adapter conformance, installed-adapter smoke matrix, authoring docs, adapter docs, semantic eval boundary, optional OpenAI runner tests, output candidate validation, JSON-RPC errors, events, usage, staging-workspace checks, redaction checks, executable policy mapping, and direct-write guards are covered | Future protocol changes should be driven by real adapter compatibility issues | post-MVP |
| Local Runners | 5 | Fake protocol fixture, official command/OpenAI/Codex CLI/Claude Code adapters, demo LLM/agent examples, and demo template flows verify protocol compatibility; provider and agent conformance is network-free by default and live calls stay opt-in | Future adapter work should focus on live opt-in examples and shared staging helpers | post-MVP |
| Project Config Surface | 5 | Project config decoding, generated JSON Schemas for `fs_project.yml` and resource files, schema drift checks against parser field contracts, runners, selectors, policies, transform declarations, local-only state backend validation, state path containment, reserved-field diagnostics, strict unknown-field diagnostics, documented aliases, free-form `meta`/`config`/`contract` objects, manifest output contract preservation, runner protocol contract passing, and documented contract/eval boundary are implemented and documented | Future config additions should remain metadata-only unless deterministic core behavior is specified and tested | post-MVP |
| Parser Baseline | 5 | Project/config/parser tests and conformance cover config versioning, aliases, refs, path containment, line diagnostics, strict YAML unknown fields, reserved removed fields, and CLI parse-error reporting through `plan`/`doctor` | Future richer schema migrations remain out of scope until specified | post-MVP |
| Manifest Graph | 5 | Manifest and graph tests cover canonical IDs, parent/child maps, deterministic JSON, selectors, upstream/downstream graph expansion, ambiguity errors, build-time manifest writes, and build integration | Future state-selection syntax remains out of scope until specified | post-MVP |
| Artifact Descriptors | 5 | Descriptor, security, policy, and diff tests cover files/directories, aggregate directory byte sizes, artifact IDs, path containment, symlink rejection, build commit use, text/Markdown diffs, and semantic descriptors | Richer descriptors for docx, xlsx, and PDF remain runner-owned | post-MVP |
| Local State | 5 | State tests cover atomic snapshots, append-only run results, failed invocation and transform receipts, locks, immutable artifact versions, eval results, policy decisions, artifact CLI inspection, orphaned artifact inspection/export semantics, read-only retention reports, high-volume retention smoke, archive-root JSON checks, `state_and_artifacts` archive unit reporting, protected current-version IDs, dry-run-required/prune-unsupported safety flags, and build commits | Future cleanup work must remain explicit, dry-run-first, current-pointer-protecting, receipt-aware, and conformance-covered | post-MVP |
| Planner Baseline | 5 | Planner tests cover run/skip/blocked actions, dirty reasons, forced rebuilds, failed-run retry reasons, selected sets, dependency-ordered selected DAGs, in-invocation upstream completion, confidence blocking, `fbt plan`, clean build reruns, failed-only reruns, and forced clean reruns | Future planner work should avoid implicit retry behavior unless it has an explicit CLI selector and receipt tests | post-MVP |
| Security Boundaries | 5 | Security/conformance checks cover schema errors, escapes, symlink rejection, runner mismatch before commit, output containment, write-scope denial, explicit agent policy parse errors, file and directory size limits, denied-commit safety, persisted policy decisions, redaction, exports, dirty propagation, secret env boundaries, redacted runner stderr diagnostics, CLI-agent staging/direct-write guards, executable CLI-agent policy mapping, negative adapter policy conformance, fail-visible staged input limits, installed-adapter smoke, documented external OS sandbox profiles, security-profile docs scan, and SDK/runtime exclusion from core | Future security work should keep stronger isolation in runner packages, CI/container profiles, or host policy rather than fbt core | post-MVP |
