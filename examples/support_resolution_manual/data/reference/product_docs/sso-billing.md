# Product Reference: SSO Billing

When SSO group sync is enabled, billable seats are calculated from active users
in the configured billing group. Seat estimates update after group sync
completes.

Billing disputes require escalation when the configured group and member list
do not match the estimate after sync.
