#!/usr/bin/env sh
# Deterministic demo runner wrapper for the checked-in example.
# Replace the matching runner entry in fs_project.yml for real provider execution.
ROOT_DIR="$(cd "$(dirname "$0")/../../.." && pwd)"
cd "$ROOT_DIR" || exit 1
exec go run ./examples/runner_adapters/demo_agent "$@"
